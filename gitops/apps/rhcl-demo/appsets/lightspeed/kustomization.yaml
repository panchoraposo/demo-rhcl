apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - resources/namespace-openshift-lightspeed.yaml
  - resources/lightspeed-operator.yaml
  - resources/olsconfig.yaml
  # (Service mesh / Kiali removed)

