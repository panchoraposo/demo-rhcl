apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../full-install
  - ../rhcl2-mcp-gateway
  - mcp-vanity.yaml
  - tracing-console.yaml
  - tls-policies.yaml
  - istio-tracing.yaml
  - istio-telemetry-gateways.yaml

patchesStrategicMerge:
  - patch-rhcl2-values.yaml

