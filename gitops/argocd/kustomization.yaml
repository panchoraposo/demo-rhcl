apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # App-of-apps entrypoint: this ApplicationSet creates the per-component Argo CD Applications.
  - applicationset-rhcl-workshop.yaml

